[package]
name = "nanoclaw-db"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock"] }
nanoclaw-core = { path = "../nanoclaw-core" }
rusqlite = { version = "0.37", features = ["bundled"] }
serde_json = "1"
